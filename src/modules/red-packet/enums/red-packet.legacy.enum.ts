export enum EnumRedPacketLegacyType {
    normal = 1,
    random = 2,
}

export enum EnumRedPacketLegacyStatus {
    normal = 0,
    unclaimed = 1,
    claimed = 2,
    expired = 3,
    refunding = 4,
}
